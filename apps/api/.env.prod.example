# Copy to apps/api/.env.prod (gitignored) for `pnpm db:sync-prod`.
# Use the prod PUBLIC connection string — Railway → Postgres → Connect → Public
# Network. The internal *.railway.internal host does NOT resolve from your machine.
PROD_DATABASE_URL=postgresql://postgres:PASSWORD@REGION.proxy.rlwy.net:PORT/railway
