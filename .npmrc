# Keep `pnpm deploy` on the legacy (symlink) behavior used by our API Dockerfile.
# pnpm 10 made non-injected workspaces require the --legacy flag at the CLI;
# pnpm 11/12 may drop the flag entirely. Setting this here decouples the
# Dockerfile from that flag and survives the deprecation.
force-legacy-deploy=true
